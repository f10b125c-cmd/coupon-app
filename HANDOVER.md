# 引き継ぎノート

## 2026-09-07 詳細画面のページ位置表示

小さな `1 / 5` は状態バッジに紛れ、何の数字か分かりにくかったため廃止。詳細画面上部に独立したナビゲーション欄を追加し、「表示中のクーポン」「1件目 / 全5件」、文字付きの「前へ」「次へ」、現在位置の進捗バー、スワイプ案内を表示する。端のボタンは消さず無効状態にして配置を固定し、1件だけなら欄自体を隠す。

数字は同一商品の枚数ではなく、現在のタブ・店舗・期限フィルターを通過した一覧内の位置。画像の変更・削除との誤操作を避けるため、ページ移動は画像に重ねない方針を維持する。隔離テストは `e2e/detail-navigation.spec.js`。

スワイプ・前後ボタンのどちらでも、現在の券が移動方向へ抜けて次の券が反対側から入るページ遷移を追加。次へと前へで方向を反転し、遷移中の連打による2件飛ばしを防ぐ。端末の「視差効果を減らす」設定（`prefers-reduced-motion`）ではアニメーションせず即時切り替える。

ページ移動欄は詳細上部ではなく商品画像／券面画像の下へ配置。画像がない券だけ商品情報の手前へ置く。詳細シート全体も画面下端揃えから上端揃えへ変更し、safe area＋8pxの余白から始まる。`100dvh`を基準に上下余白を引いて高さを制限するため、URLバーやホームバーに重ならない。

## 2026-09-06 Google Lensの手動商品名登録

詳細画面に「Google Lensで商品名を調べる」を追加。分離済み商品画像を優先してモバイルの共有シートへ渡し、未対応端末は画像コピー＋Google画像検索へフォールバックする。分離画像がない券はバーコード等を含む券面全体の外部送信を事前確認する。戻った画面で入力した候補を「この商品名を登録」で保存し、商品名・更新日時以外は変更しない。Lens結果の自動取得・自動上書き、Cloud Vision APIの有効化、課金設定は行っていない。

実地比較はプレモル、贅沢搾り、クーリッシュの3枚で商品を正しく特定し、結果確認まで約28秒・26秒・4秒。2商品券では片方だけを代表表示する場合があるため手動確認を維持する。隔離PlaywrightはLens2件を含む7件成功・実画像未指定1件スキップ、単体25件・本番ビルド成功。詳細は `CLAUDE.md` を参照。

## 2026-09-05 個別読み直しで端末文字になる再発の修正

前回確認していなかった別の実画像で、Playwrightの個別ボタン操作から保存まで再現。`350ml缶` の「缶」が `&` へ崩れ、商品名段落が不採用→英字フォールバックで端末ステータスを採用→非空の結果で無条件上書き、という連鎖が原因だった。前回の保存済みデータ修復だけでは防げていなかった。

端末/ブラウザ行の除外、再読で弱い候補を上書きしない保護、日本語専用モデルでの商品名段落の拡大再読を追加。多言語モデルの拡大では `350ml {&`、日本語モデルでは `350ml缶` と取得できることを比較確認した。元画像は端末内で処理し、新たな外部画像送信・課金は追加しない。既存の店舗・券面パターンと期限処理を維持する。

検証: 単体25件、隔離UI5件、実画像の個別再読2回を含むPlaywright 1件が成功。実画像ケースは約3分で完走し、2回とも完全な商品名・期限・メモ保持を確認。製品の認識タイマーは変更せず、実画像テスト全体の時間枠だけ420秒とした。ビルド成功。保存済みの誤読1件も競合検査付きで商品名・更新日時だけ修復し、他フィールドが変わっていないことを再取得で確認済み。

Google Lensは外部送信と自動連携の制約、代替Cloud Visionは課金設定が必要なため、懸念をユーザーへ提示して導入判断待ち。設定の有効化や画像送信はしていない。安全なPlaywright実行手順と調査の詳細は `CLAUDE.md` を参照。

## 2026-09-05 ジャパニーズエールの商品名欠けを追加修正

商品名の途中（「〈ジ／ャパニーズエール〉」「〈ジャ／パニーズエール〉」）で折り返された別画像では、容量を含む後半だけが採用されていた。また通常一括読み取りは非空の商品名を保持するため、コード更新だけでは保存済みの誤読が消えない。

`extractJapaneseAleExchangeProduct()` を独立パターンとして追加し、近接行の全文を照合する。実券の正式な説明に合わせ、対象の2種選択券を「ザ・プレミアム・モルツ／ザ・プレミアム・モルツ〈ジャパニーズエール〉夕映香るエール 350ml缶」で統一する。別商品・容量への誤補完を防ぐテストも追加。既存の店舗別ルール、通常一括読み取りの手入力保護は変更しない。

保存済みの対象5件（先頭欠け3件、端末ステータス誤読1件、前回修復した略式表記1件）を修復済み。各券の元画像を確認し、変更前の全フィールドと更新時刻を照合したうえで商品名・更新日時だけを更新。再取得で他の全フィールドが一致することを確認。手入力と思われる「プレモル」の別券は変更していない。

ブラウザの実画像OCRで完全な商品名と期限 `2026-09-22` を確認。下部説明の「〈ジャ」が実際に `(Jv` になった文字列・行座標も回帰テストへ追加した。検証端末では通常の待機上限を超えたため、ローカルの検証ハーネスだけ待機時間を延長した（約128秒で完了）。アプリのOCR回数・タイムアウト設定は変更していない。抽出テストは既存を含め22件成功。

## 2026-09-05 プレモルの商品名抽出を修正

「g ry =」は商品名を読めなかったときの英字フォールバックが拾った缶のロゴ断片。実画像のOCRでは商品名2行と容量行は読めていたが、複数行の段落を商品名として扱えていなかった。引換文を足しても「クーポン」というノイズ語で候補が落ちていた。

新しい独立パターン `extractMultilineExchangeProduct()` で、引換文の直前にある容量行と商品名段落を結び付ける。実画像の行位置・誤読文字列で、プレモル2種の正式商品名と350ml缶を取得できることを確認。従来の店舗別パターンを維持し、ロゴ断片だけなら商品名を生成しない。詳細は `CLAUDE.md` を参照。画像や利用者データはコミット・デプロイしない。

保存済みの誤読1件も修正済み。元画像・変更前の商品名・Firestoreの更新時刻を照合して商品名と更新日時だけを更新し、再取得して他のフィールドが一致することを確認した。調査時に `UNAUTHENTICATED` / `invalid_grant` が出たが、この回は鍵の失効ではなく端末とGoogleの時刻が約3時間ずれていたため。GoogleのHTTPS応答のDateを使って今回のOAuth署名時刻だけを補正し、OS設定は変更していない。

## 2026-09-03 詳細画面の画像表示順

詳細画面と編集画面では、レジで提示しやすいようにバーコードを一番上へ表示し、その下に商品画像または元のクーポン券面を表示する。通常の画像取り込みは `barcodeImageDataUrl` をバーコードとして使い、URL取り込みは `productImageDataUrl` がある場合の `imageDataUrl` を公式バーコード画像として使う。URL取り込みのバーコード画像は商品画像の下へ重複表示しないこと。

## 2026-09-02 画像クーポンのバーコード切り出し表示

画像を貼ったクーポンは、元の券面を `imageDataUrl` に残したまま、ZXingが返すバーコード両端の座標からバーコード周辺だけを切り出し、`barcodeImageDataUrl` へ別保存する。詳細画面では元画像と「バーコード」を分けて表示する。新規追加時だけでなく、1件の「読み取り直す」・未整理の一括読み取り・選択読み取りでも、切り出し画像を生成できる。数字をOCRで拾えてもZXingが線の位置を返せない画像があるため、縦に続く密な明暗線を探す画像解析も予備経路として入れている。翠ジンソーダの実画像はこの予備経路で切り出せることを確認済み。

ファミマURLの特殊取り込みは従来どおり、`productImageDataUrl` が商品画像、`imageDataUrl` が公式のバーコード画像であり、すでに分離済み。そのため `productImageDataUrl` がある券では `barcodeImageDataUrl` を重複生成しない。Firestoreの1MiB上限対策として、元画像は約700KB以下、追加するバーコード画像は約180KB以下に抑える。

位置座標がない場合の画像解析は、商品ロゴを最強候補として誤検出することがあるため、画像全体の横帯を比較する。バーコード特有の細かな黒白反復、画像幅に対して十分な線の高さ、Code128等の太い白バーをつなげる間隔を併用し、期限の赤帯・商品ラベル・端末ステータスバーを除外する。2026-09-02に既存61件を点検し、URL版など既に分離済みの券を除いた未処理49件すべてへ `barcodeImageDataUrl` を追加済み。全候補を一覧画像で目視確認し、未処理0件も再照合した。

## 2026-09-02 翠ジンソーダの商品名OCR補正

セブンの「翠ジンソーダ〈本格濃いめ〉500ml缶」の画像で、商品名先頭だけが `BYYY =H` に崩れ、後半の `〈本格濃いめ〉500ml缶` は読める事例が発生した。LINE圧縮画像では全体が `すいじんそーだ` になる事例も確認。既存の汎用抽出には手を入れず、`extractProductNameGuess()` の最終結果に対する専用補正として追加した。特徴的な `〈本格濃いめ〉500ml缶` がある場合は完全な商品名へ、読みが完全一致する `すいじんそーだ` は `翠ジンソーダ` へ直す。

## 2026-08-31 LINEの通常URL取り込み

`api/line-webhook.js` は画像だけでなく、グループに投稿されたテキストメッセージ内のHTTP/HTTPS URLも最大3件まで取り込む。URLはページのOpen Graphタイトル・画像を取得し、画像が取れれば既存の「読み取り待ち」へ、取れなければURLとタイトルだけを「未整理」へ保存する。サイトがBotアクセスを拒否した場合もURL自体は保存する。

URL取得処理は `api/_lib/lineUrl.js` に分離した。リダイレクトごとに公開HTTP URLかを検査し、ローカル名・プライベートIPを拒否する。HTMLは2MiB、画像は12MiB、1回7秒、リダイレクト4回まで。変更時はSSRＦ対策とサイズ上限を外さないこと。抽出・メタデータ処理の回帰テストは `api/_lib/lineUrl.test.js`。

ファミマの `ncpfa.famima.com/prd/ebcweb` はOG画像を持たず、ブラウザ上で追加のクーポン画面を読み込む特殊形式。`fetchFamimaCouponPreview()` で公式ページの遷移を最大2段追い、商品名・有効期限・POSバーコード画像に加え、**商品画像**も取得して、店舗を `famima` として保存する。画面の店舗マスタも `famima` のため、正式英名の `familymart` を保存すると「読み取れているのに店舗未選択」に見える。外部値は `normalizeStoreKey()` で必ず内部キーへ揃える。バーコードは `imageDataUrl`、商品画像は `productImageDataUrl` に分けて保存し、Firestoreの1MiB上限を超えないよう、ファミマ由来の画像は各340KB以下に圧縮する。URLを持つクーポンは、詳細表示・未整理の編集画面のどちらにも **「URLを開いてスキャン」** ボタンを表示する。これは元のクーポンを別タブで開き、レジでバーコードを提示するためのボタン。

商品画像URLには署名クエリが付く。署名のランダム文字列に `qr` などが偶然含まれることがあるため、商品画像・QR・バーコードの判定にクエリ文字列を使わないこと。`findFamimaProductImage()` はclass・id・alt・URLのパスだけで判定する。

詳細画面のページ移動ボタンは、画像の変更・削除との誤操作を避けるため画面上部にまとめている。画像の「変更」「削除」は画像へ重ねず、画像の下に独立した横並びの操作欄として表示する。画像上へページ移動ボタンを戻さないこと。

LINE Messaging APIには、受信済みテキスト本文を後から取得するAPIがない。対応前に投稿され、Webhookが200を返して無視したURLは復元できないため、公開後にLINEへ再投稿してもらう。

### Vercel公開時の注意（2026-08-31確認）

このプロジェクトはGitHubリポジトリとの自動デプロイ連携が未設定。**GitHubへのpushだけでは本番更新されない**。公開時は `npx --yes vercel@latest --prod --yes` を実行する。CLIが `Not authorized` になった場合は `npx --yes vercel@latest login` を実行し、表示されたデバイスログインURLをブラウザで承認する。トークンをコピー・記録する必要はない。

## 2026-08-10 LINE自動取り込みのセットアップ完了 ＋ 詳細シートの高さ修正

### LINE取り込みが本番で動くようになった

`api/line-webhook.js` は以前から実装済みだったが、環境変数が未設定で動いていなかった。今回セットアップを完了し、**家族LINEグループに画像を貼るとアプリの「未整理」に入るところまで確認済み**（ログ: `saved coupon line-626595117792231578`）。

bot: 「とみ家クーポン」`@429btbpp`

登録済みの環境変数（Vercel Production、いずれもSensitive）:
`LINE_CHANNEL_SECRET` / `LINE_CHANNEL_ACCESS_TOKEN` / `LINE_TARGET_GROUP_ID` / `FIREBASE_SERVICE_ACCOUNT_B64`

### セットアップで実際にハマった点

1. **Messaging APIチャネルはLINE Developersから作れない**（仕様変更済み）。LINE公式アカウントマネージャーの「設定」→「Messaging API」→「Messaging APIを利用する」から作る。作るとLINE Developers側にチャネルが現れる。
2. **チャネルシークレットとアクセストークンを取り違えやすい**。実際に逆に登録していて、症状が2段階で出た。
   - シークレットが違う → Webhookの［検証］が **401**（署名検証で弾かれる）
   - トークンが違う → 画像は届くが取得で **401**（`LINE content API failed: 401`）
   - 見分け方: シークレット=**16進32文字**、アクセストークン=**170文字前後**、チャネルID=数字のみ
3. **jwx / JWT / 鍵ペアの手順は不要**。あれはチャネルアクセストークン**v2.1**用。使うのは「長期」で、ボタン1つ・**有効期限なし**。「チャネル基本設定」タブの「アサーション署名キー」も同じくv2.1用なので触らない。
4. **Vercelの環境変数はSensitive扱いで読み戻せない**。`vercel env pull` してもプレースホルダしか取れないので、値の検証は登録前にクリップボード側で行う。

### 値を見ずに検証する手順（次回も使える）

```bash
# 形式チェック（値は表示しない）
python3 -c "
import subprocess,re
s=subprocess.run(['pbpaste'],capture_output=True,text=True).stdout.strip()
print(len(s), 'hex32' if re.fullmatch('[0-9a-f]{32}',s) else 'token' if len(s)>=100 else '?')"

# クリップボードから直接登録（値は経由するだけで表示されない）
pbpaste | tr -d '\n' | vercel env add LINE_CHANNEL_SECRET production
```

署名検証が通るかは、自分で署名を作って叩けば確認できる（LINE側の［検証］ボタンを押さなくてよい）。

```bash
python3 -c "
import subprocess,hmac,hashlib,base64,urllib.request
s=subprocess.run(['pbpaste'],capture_output=True,text=True).stdout.strip()
b=b'{\"events\":[],\"destination\":\"Utest\"}'
sig=base64.b64encode(hmac.new(s.encode(),b,hashlib.sha256).digest()).decode()
r=urllib.request.Request('https://coupon-app-dusky.vercel.app/api/line-webhook',data=b,
  headers={'Content-Type':'application/json','x-line-signature':sig},method='POST')
print(urllib.request.urlopen(r).status)"
```

アクセストークンの有効性は `GET https://api.line.me/v2/bot/info` に Bearer で投げれば200が返る。

### 詳細シートが画面上部でURLバーに隠れる問題（App.jsx `useSheetStyles`）

原因は2つ重なっていた。

1. `maxHeight: "90vh"` を使っていた。スマホの `vh` は**URLバーが引っ込んだ状態**の高さなので、実際の表示領域をはみ出してシート上端がURLバーの裏に入る。→ 現在の表示高さを表す **`dvh`** に変更（未対応端末向けに `vh` も残す）。
2. `max-height` は既定(content-box)ではパディングを含まないため、上下48pxぶん指定より背が高くなっていた。→ **`box-sizing: border-box`** を指定。

375×812で実測: シート上端 33px → **97px**。あわせて下端に `env(safe-area-inset-bottom)` を追加（ホームバー・下部ツールバー対策）。

インラインstyleはクラスより強いので、`.sheet` 側で指定する `max-height` と `padding-bottom` を**インラインに書き戻さないこと**。

## 2026-07-29 商品名OCRの誤読対策 ＋ スワイプ終了確認 ＋ 選択して読み取り直す

### 背景（何が起きていたか）

サントリー「-196 〈ダブルレモン〉 5% 350ml缶」のセブンイレブンクーポン2枚で、商品名が壊れて登録された。

| クーポン | 修正前の商品名 |
|---|---|
| 2371-1581-62673（1枚目） | `r-196<ダブルレモン> 5% 350ml缶` |
| 2320-2196-71661（2枚目） | `1196(¥ 7ILL EV)5% 350ml缶` |

### 原因

実画像でOCRの生出力を取って特定した。原因は3つ重なっていた。

**原因1: 開き鉤括弧「が文字として誤読される**

商品名は `「◯◯」1本と引き換え` の鉤括弧で切り出しているが、`「` は細い縦棒なのでOCRが `1` や `r` と読む。閉じの `」` は正しく読めるため、`「(.+?)」` のパターンだけが一致せず、化けた1文字が商品名の先頭に残っていた。

実測されたOCR出力:
```
1枚目: 1-196 < ダブ ルレ モン >〉 5% 350ml 缶 」1 本 と 引き 換
2枚目: 1196 〈 く ダブ ルレ モン )〉 5% 350ml 缶 」1 本 と
```

**原因2: 括弧の字形取り違えと二重取り**

`「`→`『`、`〈`→`《`、`〉`→`)〉`、`〈`→`〈く` のように、似た字形への取り違えと、括弧を2文字ぶん拾う現象が起きる。

**原因3: 缶のデザイン・ロゴを商品名として拾う（2枚目が読めなかった直接の原因）**

`scanText` は画像を canvas 経由で JPEG に再エンコードしてからOCRにかけるため、**JPEGエンコード結果が端末ごとに違い、OCR結果も割れる**。開発機（デスクトップChrome）では2枚目も `ダブルレモン` と読めたが、ユーザーの端末では缶のロゴを拾って `(¥ 7ILL EV)` になっていた。

従来の妥当性チェック `hasEnoughNameChars` は「数字・単位語を除いて3文字以上」だったため、`¥ILLEV%` のような英数字の羅列を通してしまい、ページ下部に再掲されている正しい商品名まで到達できなかった。

### 修正内容（src/scan.js）

1. **`normalizeBrackets`** — 判定前に括弧の字形を揃える。`『｢`→`「`、`』｣`→`」`、`《＜<`→`〈`、`》＞>`→`〉`。
2. **`tidyBrackets`** — 重なった括弧（`〈く…`、`)〉`、`〉〉`）と、相方のない括弧を落とす。
3. **`recoverFromBrokenOpenBracket`** — 閉じ `」` があるのに開き `「` が無い行は「が化けたとみなし、`」` の手前までを商品名として先頭1文字を捨てる。**ハイフンは商品名そのものの先頭（-196）でありうるので捨てる文字集合から除外**している。
4. **`hasEnoughNameChars` を日本語の文字数で判定するよう変更** — 単位語（缶・本・ml等）を除いた**ひらがな・カタカナ・漢字が3文字以上**を要求。コンビニクーポンの商品名は必ず日本語を含むが、ロゴ誤読は英数字と記号ばかりになるため。これを `cleanProductLine` の最終チェックに入れ、全ルールに効かせた。結果、ロゴ誤読の行が棄却され、ページ下部に再掲された商品名を拾い直せる。
5. **ノイズ判定を定型文の除去より前にも実施** — `※賞品の引換えはお一人様一回のみ` は、尻尾（引換え以降）を落としたあとだと `賞品の` しか残らずノイズ語が消えて商品名と誤認されていた。鉤括弧で区切られていない行に限り、除去前にノイズ判定する。あわせて `賞品` `当選者` `引換えは` をノイズ語に追加。
6. **`tidySpacing`** — 半角英数と日本語の間の空白も詰める（`350ml 缶`→`350ml缶`）。

### 修正後の結果

| クーポン | 修正後 |
|---|---|
| 2371-1581-62673（1枚目） | `-196〈ダブルレモン〉5% 350ml缶` ✅ |
| 2320-2196-71661（2枚目） | `196〈ダブルレモン〉5% 350ml缶` ⚠️先頭の `-` は欠落 |

**2枚目の `-` が復元できない理由**: OCRが `「-196` を `1196` としか読んでおらず、ハイフンの情報自体が失われている。`「`が化けた1文字を捨てると `196` になる。2文字捨てると `96` になり悪化するため、ここは手入力で補うしかない。ページ下部の再掲行も `196` でハイフンが無く、他に手がかりが無い。

### 検証

`extractProductNameGuess` に9パターンのOCR出力を与える回帰テストを実施（すべて期待どおり）。

| ケース | 結果 |
|---|---|
| 税込価格つき（ファミマ） | `アサヒ生ビール缶 350ml` |
| 鉤括弧が正常に読めた場合 | `キリンのどごし〈生〉350ml缶` |
| 山括弧つき商品名 | `翠ジンソーダ〈すっきり爽やか〉5% 350ml缶` |
| 括弧なし引き換え行 | `淡麗プラチナダブル 350ml缶` |
| 1枚目実データ | `-196〈ダブルレモン〉5% 350ml缶` |
| 2枚目・開発機のOCR | `196〈ダブルレモン〉5% 350ml缶` |
| 2枚目・端末のロゴ誤読＋下部に再掲あり | `196〈ダブルレモン〉5% 350m缶` |
| 全部ロゴ誤読で手がかり無し | `""`（誤った名前を付けるより空を返す） |
| ノイズ行に `」` がある（LINEバナー） | `サントリー生ビール 500ml缶` |

加えて、Firestoreの実クーポン4件の画像で実際にOCRを流して確認済み。

### 追記: 上記の修正で別の誤検出が出たため追加対応

上の修正を入れた直後、2枚目の商品名が `ださい TE` になった。

**原因**: ロゴ誤読を弾けるようにした結果、これまで採用されていた候補が棄却され、フォールバック（ルール4「残った候補の1行目」）まで落ちて、ページ上部のLINEバナー「…のスキャンをお申し付けください。」を誤読した断片を拾ってしまった。`ださい` は日本語3文字なので妥当性チェックを通過し、ノイズ語 `ください` は**OCRが「く」を落としていたため一致しなかった**。

**対応（3点）**:

1. **フォールバックを保守的にした（再発防止の要）** — ルール4を「単位語（ml/缶/本など）を含む行だけ」に限定し、**該当が無ければ空を返す**。従来の「残った候補の1行目」を採用する挙動を廃止。誤った名前を付けるより空にして手入力に委ねる方が実害が小さい。
2. **ノイズ語を部分一致に寄せた** — `ください` → `ださい`（`ください`にも部分一致する）。あわせて `申し付` `スキャン` を追加。
3. **OCRに渡す画像をJPEGからPNGに変更（根本原因）** — `scanText` は canvas から `toDataURL("image/jpeg", 0.9)` していたが、保存済み画像はすでにJPEG圧縮済みなので**二重圧縮**になっていた。加えて**JPEGエンコーダの出力は端末ごとに異なるため、同じ画像でも端末によってOCR結果が変わる**（開発機で再現しない不具合の正体）。可逆なPNGにして差をなくした。実画像4件で処理時間は約10〜12秒で従来並み。

**検証**: `ださい TE` を含む行構成2パターンで空が返ることを確認。既存8パターンの回帰も維持。実クーポン4件でも `ださい TE` → `196〈ダブルレモン〉5% 350ml缶` に復旧。

### E2Eテスト（2026-07-29 追加）

`npm run e2e`（ローカルdevサーバ / `npm run dev` を先に起動）、`npm run e2e:prod`（本番URL）。

`e2e/rescan.spec.js` に2本。実物のクーポン画像 `e2e/fixtures/coupon2-doublelemon.jpg`（2320-2196-71661）を使う。

1. **壊れた商品名がOCRで正しい商品名に上書きされる** — 商品名を `ださい TE` にしたクーポンを仕込み、UIでチェック→「読み取り直す」を実行。商品名に `ださい`/`TE` が含まれないこと、`ダブルレモン` を含むこと、バーコード `2320219671661` と店舗 `seven` が入ることを検証。
2. **読み取れなかったときに注意書きやUI文字を商品名にしない** — ノイズ行だけ・注意書きだけを渡したときに空が返ることを検証。

**注意点**:
- E2Eは**実際の家族共有Firestoreに書き込む**。テストデータのidは `tagtest_` 接頭辞で、`afterEach` で必ず削除している。この規約は崩さないこと。
- OCRの完了待ちは**完了トースト（「読み取り直しました」）で待つ**こと。進捗表示「読み取り中…」の消滅で待つと、表示される前に判定が通ってしまい、スキャン前の値を読んで**誤って失敗扱いになる**（実際にこれで1回ハマった）。

### 同時に入れた変更

**選択して「読み取り直す」（App.jsx `bulkScanSelected`）**
どのタブでもカードのチェックを付けて、下部バーの「読み取り直す」でまとめて再スキャンできる。**商品名だけは意図的に上書きする**（OCR改善後に既存の壊れた名前を直す用途のため）。店舗・期限・バーコードは手で直している可能性があるので空のときだけ埋める。

**スワイプでの終了確認（App.jsx `useExitConfirm`）**
ホーム画面に追加したアプリで左スワイプ（戻る操作）すると確認なしで閉じてしまうため、履歴にダミーを1件積んで `popstate` を受け止め「クーポン管理を終了しますか？」を出す。当初はPWA判定でゲートしていたが、判定が効かない端末で素通りするため**常に有効**にした。このアプリは画面遷移を持たない1画面構成なので、戻る＝アプリを離れるとみなしてよい。

### 残っている注意点・未対応

- **2枚目の先頭ハイフン**は上記の理由で自動復元できない。手入力で補う。
- **OCR結果が端末依存になる問題はPNG化で対処したが、完全に同一になる保証は無い**（tesseract.jsのWASMビルドやフォント描画の差が残る）。実機の症状を疑うときは、Firestoreに保存済みの `imageDataUrl` を使って開発機でOCRを流し、生の行テキストを見るのが早い。
- **商品名が読めないときは空になる**（誤った名前を付けない方針）。`bulkScanSelected` は `detectedName || c.productName` なので、空が返ると**既存の名前が残る**。誤った名前が既に入っている場合は、詳細画面で手入力して直す必要がある。完了トーストで「N件は商品名を読み取れませんでした」と件数を出しているので、そこで気付ける。
- **「直したはずなのに端末で直らない」ときは、まずPWAのキャッシュを疑う**。ヘッダーの「更新 M/D」がビルド日なので、そこが最新の日付になっていなければ古いバンドルが動いている。アプリを完全に終了して開き直す。本番に修正が乗っているかは
  `curl -s https://coupon-app-dusky.vercel.app/ | grep -oE 'assets/index-[A-Za-z0-9_-]+\.js'` でバンドル名を取り、そのファイルを `curl` して目印の文字列を `grep` すれば確認できる。
- **英語だけの商品名は弾かれる**。`hasEnoughNameChars` が日本語3文字以上を要求するため。国内コンビニクーポン前提の割り切り。
- **使用済みクーポン `cj23mdyfmrc7nzft` の再スキャン結果に末尾 ` 1` が残る**（`サントリー生ビール / サントリー生ビール(FEE)500ml缶 1`）。商品名の行と「1本と引き換え」の行がOCRで分かれたときに、数量の `1` だけが行末に残るケース。実害が小さいので未対応。
- **終了確認で「はい」を選んだとき**、`history.back()` を呼ぶが、アプリを新規起動して履歴が1件も無い状態では戻り先が無く閉じないことがある（Androidのホーム画面アプリでは閉じる想定）。
- **詳細モーダルを開いた状態で左スワイプ**すると、モーダルを閉じるのではなく終了確認が出る。今回の要望どおりの挙動だが、「モーダルが開いていれば先に閉じる」に変えることは可能。

# coupon-app 引き継ぎ

このファイルは、次回このリポジトリを扱う Claude / Codex 向けの作業メモです。
挙動を変更した場合は、コード・テストと一緒にここも更新してください。

## 現在の構成

- React 18 + Vite の PWA
- Firebase Authentication（匿名認証）+ Cloud Firestore でクーポンを共有保存
- Tesseract.js（`jpn+eng`）で商品名・期限をOCR
- ZXingでバーコードを検出し、OCRした印字数字もフォールバックに使用
- 本番: https://coupon-app-dusky.vercel.app
- ヘッダーの家族ポータル: https://tomifufu-family-home.aorin2025.chatgpt.site
- GitHub: https://github.com/f10b125c-cmd/coupon-app.git
- 本番ブランチ: `main`

主なファイル:

- `src/App.jsx`: 登録、再読み取り、一括読み取り、保存、表示
- `src/scan.js`: OCR、期限・商品名・バーコード・店舗の抽出
- `src/scan.test.js`: 抽出ロジックの回帰テスト
- `src/firebase.js`: Firebaseクライアント初期化
- `api/line-webhook.js`: LINE画像・URLの自動取り込み
- `api/_lib/lineUrl.js`: URL抽出、ページタイトル・OG画像取得、外部URLの安全検査

ヘッダーの家族ポータルリンクは別タブで開く。
幅359px以下ではラベルを隠してアイコンだけにし、更新ボタンとの横並びを維持する。

LINEグループの通常URLは、タイトルとOG画像の取得を試して未整理へ保存する。画像が取れれば読み取り待ち、取れなければURL項目として残る。受信済みのテキスト本文はLINE APIから後取得できないため、機能追加前のURLは再投稿が必要。

ファミマの `ncpfa.famima.com/prd/ebcweb` は例外で、OG画像ではなく追加画面から商品名・期限・POSバーコード画像を取得する。`fetchFamimaCouponPreview()` の遷移先制限（公式の`/prd/`・`/contents/`だけ）を緩めないこと。

## 開発・確認・公開

```bash
npm install
npm run dev
npm test
npm run build
```

本番公開はVercel CLIで行う。

```bash
npx --yes vercel@latest --prod --yes
```

このVercelプロジェクトはGitHub pushだけでは自動公開されない。`Not authorized` が出た場合は、先に `npx --yes vercel@latest login` を実行し、表示されるVercelのデバイスログイン画面で承認してから再実行する。Vercelの認証値は文書や`.env`へ保存しない。

公開後は `https://coupon-app-dusky.vercel.app` が新しいデプロイへエイリアスされたことを確認する。
PWAのService Workerが旧JavaScriptを保持することがあるため、スマホ確認時はアプリを完全に閉じて開き直す。

## OCR処理の流れ

### 2026-09-09 同じ商品をまとめる並び順

並び順へ「同じ商品をまとめる」を追加し、この機能の初回表示だけ既存の端末設定から新しい並びへ切り替える。以後にユーザーが「期限が近い順」「追加が新しい順」「追加が古い順」を選んだ場合は、その端末の選択を `localStorage` に保持して上書きしない。

`src/couponSort.js` の `normalizeProductGroupKey()` は、商品名のNFKC正規化と小文字化を行い、空白・括弧などの記号に加えて、券面ごとに付き外れする「お持ち帰り限定」「税込価格」「無料引換券」、主要メーカー名、末尾に単独で残った「1本」を比較時だけ除く。さらに `ml` / `L` / `g` / `kg` の容量・重量と直後の容器表記を比較時だけ除き、翠ジンソーダ350ml缶と500ml缶を同じグループにする。保存された商品名自体は変更しない。味・度数など商品差になる文字は残すため、5%と7%、バニラとチョコは別グループになる。「1本満足バー」のように数量表記の後ろへ商品名が続く場合は除去しない。

同じキーの商品は必ず連続させ、グループ間はその商品の最も近い期限、グループ内も期限が近い順で並べる。商品名未設定は同じ商品と断定せず1件ずつ独立させ、追加が新しい順にする。一覧の `filtered` をこの順にするため、詳細画面の左右スワイプ順も同じになる。データの保存順やFirestoreの内容は変更しない。

`src/couponSort.test.js` は表記ゆれ、容量違い、味・度数違い、連続配置、商品名未設定を検証する。`e2e/product-grouping.spec.js` は家族のFirestoreを遮断し、新しい並びが初回既定になること、クーリッシュ、タコハイ、翠ジンソーダの表記違いが一覧とスワイプ順の両方で隣接することを確認する。2026-09-09時点で `npm test` は30件成功、`npm run e2e:offline` は12件成功・実画像任意テスト2件スキップ、`npm run build` は成功。

### 2026-09-07 詳細表示時のバーコード自動切り出し

通常の画像クーポンで `barcodeImageDataUrl` または `couponPreviewImageDataUrl` がまだ保存されていない場合、詳細を開いた時点で `scanBarcodeWithCrop()` だけを自動実行し、バーコードと商品確認用プレビューを切り出して表示する。成功時は元の `imageDataUrl` を残したまま不足していた切り出し画像と更新日時だけをクラウド保存するため、次回から再解析しない。処理中は「画像を見やすく整えています…」と表示する。開発時のReact StrictModeによるEffect再実行でも解析が二重にならないよう、同一コンポーネント内では解析Promiseをrefで共有する。

番号を復号できればZXingの検出座標からバーコードを切り出し、復号できなくても既存の黒白縦線密度による予備検出を使う。商品プレビューの位置には黒白縦線密度で得た元画像上のバーコード位置を使う。両方の派生画像が保存済みの券、ファミマURLのように `imageDataUrl` 自体が公式バーコード画像の券、未整理を開いた直後に全体読取を始める券では重複実行しない。検出できなければ元画像表示を維持し、商品名・期限・店舗等は変更しない。

`e2e/barcode-auto-crop.spec.js` は家族のFirestoreとOCRを遮断し、未切り出し券を開くと1回だけ検出し、バーコード・商品プレビュー・元画像を表示／保存し、商品名・期限・メモを保持することを検証する。実際の画像解析は `src/scan.test.js` の縦線群検出と上下領域計算に加え、任意のローカル実画像をChromiumへ渡すケースで確認する。

### 2026-09-08 スワイプ専用のページ移動と商品プレビュー

詳細画面の「前へ」「次へ」ボタン、進捗バー、スワイプ案内は削除した。左右スワイプと従来のスライド演出は維持し、現在位置はステータス（未使用・未整理等）の右隣にある「ページ 1 / 5」のチップだけで示す。この件数は同一商品の重複枚数ではなく、選択中のタブ・店舗・期限フィルターを通過した現在の一覧件数。1件だけならページチップは出さない。`e2e/detail-navigation.spec.js` はボタンが存在しないこと、390×844でページ位置・バーコード・商品画像が初期viewport内にあること、タッチイベントから `idle → leave-next → enter-next` で2件目へ移ることを検証する。

空いた高さを使い、商品／券面画像の上限を `min(38dvh, 310px)` へ拡大した。通常のスクリーンショット券は、バーコードの黒白縦線群を検出できた場合、その上下で広い側を商品確認用に自動切り出し、`couponPreviewImageDataUrl` へ別保存する。セブンはバーコード下、ローソンは上に商品があるため、固定位置や店舗名ではなく画像内の空き領域で方向を決める。商品写真と商品名が一緒に入るよう、最大で元画像幅の1.35倍の高さを使う。小さすぎる候補は採用しない。

元の `imageDataUrl` は削除・上書きせず、編集画面では従来どおり券面全体を確認できる。保存済み券も詳細を開いた時にプレビューがなければ生成・保存する。新規追加、個別再読、未整理の一括読取、選択読取でも同じ結果を保存する。自動切り出し中は「画像を見やすく整えています…」と表示する。失敗時は元の券面表示へ戻し、商品名・期限・店舗などは変更しない。

Firestoreの1MiB上限を守るため、元画像は従来の約700KB以下、バーコード画像は約180KB以下、商品プレビューは90KB以下に抑える。商品プレビューがあればGoogle Lensにも券面全体ではなくプレビューを優先して渡す。ローソンのクーリッシュとセブンのプレモル実画像を隔離Chromiumで解析し、それぞれバーコードの上側・下側の商品領域を取得、90KB以内に収まることを目視確認した。実画像はGitへ含めず、`COUPON_CROP_IMAGES=/path/one.jpg:/path/two.jpg npm run e2e:offline` で任意追加検証する。

確認結果は単体26件成功、隔離E2Eは通常10件成功・任意実画像2件スキップ。実画像2枚を指定した商品切り出しテストも別途1件成功し、本番ビルドも成功した。

詳細シートは上下のsafe areaと8pxの余白を確保し、`100dvh`基準で画面上端から表示する。閲覧時のバーコードは `min(18dvh, 140px)` のまま。連続スワイプ中の二重移動はrefで防止し、`prefers-reduced-motion: reduce` では待機時間とCSSアニメーションを無効化する。

### 2026-09-09 上スワイプで使用済み・次の券へ移動

閲覧中の詳細シート上部に専用の取っ手を追加し、そこから60px以上かつ横移動より明確に大きく上へスワイプすると現在の券を使用済みにする。シート本体は縦スクロールできるため、通常スクロールで誤操作しないよう上スワイプ判定は取っ手から開始したジェスチャーだけに限定する。編集時と使用済み券では取っ手を表示しない。

親の `markOpenCouponUsedAndAdvance()` は更新前の `filtered` から次の券を確保し、現在の券を使用済み保存してから `leave-used → enter-used` の縦方向アニメーションで次の詳細へ移る。「使用済みにする」ボタンも同じ処理を使う。次の券がない最後の1枚は、使用済み表示へ変えて詳細を閉じずに残す。`prefers-reduced-motion` では従来どおりアニメーションを省略する。`e2e/detail-navigation.spec.js` は上スワイプ、ボタン、最後の1枚、保存内容、詳細が閉じないことを家族のFirestoreから隔離して検証する。

### 2026-09-06 Google Lensの手動商品名登録

商品部分だけをGoogle Lensへ送る実地比較では、プレモルを約28秒で「ザ・プレミアム・モルツ〈ジャパニーズエール〉夕映香るエール 350ml缶」、贅沢搾りを約26秒でマスカット・みかんテイストの両方と350ml、クーリッシュを約4秒で「ロッテ クーリッシュ バニラ 140ml」と特定できた。時間は検索結果の画面確認を含む参考値。2商品券ではLensが一方だけを代表表示することがあるため、自動保存には使わない。

詳細画面に「Google Lensで商品名を調べる」を追加。`src/lensSearch.js` は次の順で画像を渡す。

1. `productImageDataUrl` があればURL取り込み等の商品画像だけを使用する。
2. なければ `couponPreviewImageDataUrl` の自動切り出し商品部分を使用する。
3. モバイルでファイル共有に対応していれば共有シートを開き、ユーザーがGoogle / Google Lensを選ぶ。
4. ファイル共有に未対応なら商品画像をPNGでクリップボードへコピーし、Google画像検索を別タブで開く。コピーもできなければ手動アップロードを案内する。
5. Lensから戻った詳細画面で候補名を入力し、「この商品名を登録」を押したときだけ `productName` と `updatedAt` を更新する。期限・店舗・バーコード・メモ・画像は変更しない。

分離商品画像がない券は、バーコード等を含む券面全体がGoogleへ送られる可能性を確認ダイアログで明示し、承認された場合だけ続ける。Lens結果をアプリへ直接取得する公開公式APIは確認できないため、検索結果の自動抽出や自動上書きは実装しない。Cloud Vision APIは未有効のままで、課金設定も変更していない。

`e2e/lens-manual.spec.js` はWeb Shareを模擬し、分離画像の優先、未分離画像の送信前確認、手動登録、他フィールド保持を検証する。`npm run e2e:offline` は開発サーバーを自動起動し、家族のFirestore通信を遮断した7件が成功（実画像を指定しない追加OCRケース1件は意図どおりスキップ）。単体25件と本番ビルドも成功。

### 2026-09-05 個別再読で「ul docomo会」へ戻る問題

前回「プレモル」の手入力名を保持した別画像が対象。Playwrightで本物の詳細画面の「読み取り直す」を押し、正しい商品名が `ul docomo会 11:50 1¢@45%@` へ上書きされることを再現した。商品名2行は読めているが、容量行が `350ml &`、下部の別表記も `〈ジャパニーズエール)〉 HME I—IL 350m向1` に崩れ、従来の全文パターンに一致しなかった。英字も許すフォールバックが先頭の端末ステータスを採用し、再読は非空なら無条件で保存していた。キャッシュだけの問題ではない。

対策は既存の券面パターンの前後に追加:

- 通信会社・時刻・電池・URLの行を商品名候補から除外。
- 構造化された商品名、容量や価格・引用などの根拠がある候補と、弱いフォールバックを区別。
- `extractProductNameForRescan()` を個別・選択再読の保存境界に使用。根拠の弱い別候補で非空の既存名を上書きしない。保持した場合は画面にも明示する。
- 弱い候補しかない場合のみ、容量と引換文から `findProductNameRetryRect()` で商品名段落を特定して最大3倍・幅2000px以内へ拡大。日本語専用モデルで1回再読し、根拠のある結果だけ採用する。失敗時も元の結果・既存名を維持する。
- 多言語モデルのまま拡大すると `350ml {&` のままだったが、日本語モデルでは同じ領域を `350ml缶` と取得できた。`&` を缶へ文字列置換する対応ではない。再読後は `jpn+eng` に戻し、既存期限OCRの言語は維持する。

#### 家族データに触れないPlaywrightテスト

`npm run e2e:offline` は `playwright.offline.config.js` で新しい隔離テストだけを選ぶ。保存層をメモリへ差し替え、Firebase通信を遮断。端末文字・ロゴ断片の拒否、個別/選択ボタン、正常な商品名の更新を検証する。

実画像はGitへ含めず、`COUPON_RESCAN_IMAGE=/ローカルの実画像.jpg npm run e2e:offline` で追加する。実画像ケースではOCRもバーコードも差し替えず、個別ボタンを2回押して修復・再発防止・期限/メモ保持まで検証。製品コードのタイムアウトは延長しない。従来の `npm run e2e` は実Firestoreへ接続する別テストなので、代わりに実行しないこと。

確認結果: 単体テスト25件、隔離した画面操作テスト5件が成功。実画像の連続再読テストも成功し、2回とも `ザ・プレミアム・モルツ／ザ・プレミアム・モルツ〈ジャパニーズエール〉夕映香るエール 350ml缶` になり、期限・メモを保持した。実画像ケースは2回の実OCRを含むためケース全体の上限を420秒に設定（実行約3分）。アプリの既存認識タイマーは延長していない。ビルドも成功。

保存済みの端末文字になっていた対象1件も修復済み。実画像と更新時刻を照合し、競合条件付きで商品名・更新日時のみ変更した。再取得して画像・期限・バーコード・メモなど他フィールドが不変であることを確認。診断画像・元データ・認証情報はGitにもデプロイにも含めない。

#### Google Lensの検討（ユーザー判断待ち）

自動採用は保留。Lens検索結果をアプリへ返す公開公式APIは確認できず、手動Lensは画像をGoogleへ送る。画像検索履歴がオンの場合、保存画像は認識技術の改善等に使われる可能性がある。公式の代替候補Cloud Vision Web Detectionは別サービスで、課金設定・API有効化が必要。確認時の料金は月1,000ユニット無料、1,001〜5,000,000は1,000ユニットあたりUS$3.50。他のクラウド費用は別。APIを有効化したりクーポン画像を送信したりする前に、ユーザーの判断を待つ。

根拠: https://support.google.com/websearch/answer/14601082?hl=ja 、https://docs.cloud.google.com/vision/docs/detecting-web 、https://cloud.google.com/vision/pricing

### 2026-09-05 ジャパニーズエールが欠ける問題（追加対応）

別の実画像では、下部説明が「〈ジ／ャパニーズエール〉」「〈ジャ／パニーズエール〉」の途中で折り返されていた。保存値には `ャパニーズエール夕映香るエール 350ml和缶 1本`、`ヤャパニーズエール夕映香るエール 350ml缶 1本`、`パニーズエール夕映舌るエール 350ml缶` が残っていた。容量を含む後半の行だけが選ばれたことと、通常の一括読み取りが既存の商品名を優先することが、修正後も誤読が残る理由。

既存パターンを変更せず、`extractJapaneseAleExchangeProduct()` を前段に追加。近接する最大4行で「ザ・プレミアム・モルツ／ザ・プレミアム・モルツ」「夕映香る（舌る）エール」「350ml缶」を全文照合し、実券の下部説明で確認した `ザ・プレミアム・モルツ／ザ・プレミアム・モルツ〈ジャパニーズエール〉夕映香るエール 350ml缶` へ揃える。画像内の略式キャプションにシリーズ名がなくても、この2種選択券に完全一致する場合だけ同じ表記へ補完する。別容量・別のエール・離れた段落やロゴ断片だけには適用しない。

通常の一括読み取りの既存値優先は、手入力の商品名を守るため維持する。新規取込・詳細の再読取・選択読取は同じ抽出関数を使用する。過去の誤読はコードを公開しただけでは自動修復されないため、対象画像を確認したうえで個別に修復し、商品名と更新日時以外を変更しないこと。

### 2026-09-05 プレモルが「g ry =」になる問題

原因はOCR後の商品名抽出。実画像では「ザ・プレミアム・モルツ／」「ザ・プレミアム・モルツ 夕映舌るエール」「350ml缶」の3行が読めていたが、従来の単位語優先ルールでは商品名2行を採用できず、容量行だけでは日本語文字数が足りずに失敗していた。さらに引換文を連結すると、定型文除去より前の「クーポン」ノイズ判定で落ち、最後の緩いフォールバックが缶のロゴ断片を拾っていた。保存値は `g ry =`、ローカルの同じ画像での再現値は `| をシ p p`（端末・OCR結果で断片は変わる）。

`extractMultilineExchangeProduct()` を独立した新パターンとして追加。「商品名の近接行→容量だけの行→1本無料引換えクーポン」を読む。既存のローソン・鉤括弧などのパターンは維持。プレモル2種350mlの全文が一致するときだけ「夕映舌る」を「夕映香る」へ補正する。`g ry =` 自体をプレモルへ置換してはいけない。等号・縦棒を含む短いロゴ断片しかない場合は空を返し、英字商品名（Red Bull、Coca-Cola）は維持する。追加OCRは不要。

実画像の `scanText()` 結果へ新抽出処理を適用し、「ザ・プレミアム・モルツ／ザ・プレミアム・モルツ 夕映香るエール 350ml缶」になることを確認。実際の行座標と文字列を回帰テストへ収録した。調査画像・FirestoreバックアップはGit無視の `diagnostics.local/` 内だけに置き、公開対象にも含めない。

`scanText()` は次の順で画像を読む。

1. 画像全体を最大2000pxへ縮小し、PNGで通常OCR
2. 色付き帯の白抜き商品名を拾うため、反転画像もOCR
3. 全体から期限を取得できなかった場合のみ、券面下部を拡大して期限OCR
4. それでも取れない場合は下部を二値化して数字中心にOCR
5. 最後に赤画素の最下段を検出し、赤い期限行だけを抽出してOCR

期限専用OCRを最初から実行しないのは、すでに読める券の処理時間とモバイル負荷を増やさないため。
商品名の行情報には期限専用OCRの結果を混ぜず、期限抽出用の全文にだけ追加する。

`extractExpiryDate()` は通常日付パターンより先にローソンの店舗利用期限を確認する。
期限欄は `23:59` をアンカーにし、以下のようなOCR崩れを補正する。

- `2026/08/24 23:59まで`
- `2026/08/2423559 ま で`
- `202608242359`
- `2026108242359 xc`（区切りの誤認識と「まで」の崩れ）
- `20267087/2423:59`（スラッシュが数字へ化けたもの）

期限欄で現在年から大きく外れた年は `normalizeDeadlineYear()` で現在年へ補正する。
これは `2075/08/24` のように `2026` を誤認したケースへの対応で、通常の日付抽出全体には適用しない。

## 2026-08-19 ローソン期限OCR修正

症状:

- 商品名やバーコードは読めても店舗利用期限が空欄になる
- 一部で `2026` が `2075` になる
- 同じレイアウトでも読める券と読めない券がある

原因:

- 期限は券面最下部の小さな赤文字で、画像全体のOCRでは落ちやすい
- 拡大時の画像補間で細い数字がぼける
- 広い下部クロップでは、OCRがバーコード下の17桁数字へ集中する
- 区切りや末尾の「まで」が崩れると従来パターンに一致しない

実装した対策:

- 拡大時は `imageSmoothingEnabled = false`
- 通常、二値化、赤文字だけのクロップを段階的に使用
- `findDeadlineRedBand()` が券ごとに期限行の位置を決定
- 赤文字OCRは数字と区切り文字に限定し、1行として読む
- 「まで」が読めなくても `23:59` があれば期限候補として扱う
- 区切りが数字になっても、有効な月日を後ろ側から復元

保存済みの失敗画像（クーリッシュ、アイスの実、ガリガリ君）を、
Playwright + Chromium上の実際の `scanText()` に通した。
3枚とも `2026-08-24` を取得し、ユーザーの実機でも読み取り成功を確認済み。
Nodeだけでなく、アプリと同じCanvas/Tesseract経路で確認することが重要。

関連コミット:

- `f2e77f7`: 一括読み取りの期限対応
- `e4917e8`: 期限欄の拡大OCR
- `e3eb94f`: 期限上書きと不自然な年の補正
- `da36c6d`: 赤期限行検出と区切りなし期限の復元

## 再読み取りと期限上書き

- 詳細画面の再読み取り: 検出した期限を既存値より優先
- 選択読み取り: 検出した期限を既存値より優先
- 未整理の通常一括読み取り: 既存期限を優先し、空欄だけ補完

期限が空欄なら通常一括読み取りでも対象になる。
`2075` のような誤った非空欄値を直す場合は「選択読み取り」を使う。
単体・選択は `detectedDate || existing`、通常一括は
`existing || detectedDate` の順序。変更時は意図を確認すること。

## 変更時のルール

- 既存店舗・既存券面の抽出パターンを安易に置換しない
- 新しい券面は独立パターンとして追加し、既存テストを維持する
- 実際に崩れたOCR文字列を `src/scan.test.js` へ追加する
- 画像前処理は、可能なら実ブラウザの `scanText()` でも確認する
- OCR失敗時も商品名や既存期限を空文字で消さない
- 高負荷な追加OCRは通常OCR失敗時だけ行う

## セキュリティ

- Firebase AdminのサービスアカウントJSONはローカル検証専用
- 秘密鍵、アクセストークン、画像データ、利用者データをコミットしない
- `.gitignore` は `.env*`、サービスアカウントJSON、Vercel情報を除外している
- Firestoreを直接調査する場合も、ユーザーの明示許可なしに保存値を変更しない

### GitHub PAT

- HTTPS認証はmacOSの `osxkeychain` credential helperを使用する
- 現在のPATは2026年9月18日までの有効期限で発行されている
- 2026年9月18日以降にpushが認証エラーになった場合は、PAT期限切れを最初に確認する
- 必要ならGitHubでPATを再発行し、macOSキーチェーンのGitHub認証情報を更新する
- PATの値そのものは、文書・`.env`・コミット・チャットへ記録しない
- 認証確認は `GIT_TERMINAL_PROMPT=0 git ls-remote origin HEAD` で安全に行える

## 完了前チェック

1. `npm test`
2. `npm run build`
3. `git diff --check`
4. 必要に応じて実ブラウザ・実画像でOCR確認
5. コミット、`main`へpush、本番デプロイ
6. 本番HTMLが新しいハッシュ付きJavaScriptを参照していることを確認
